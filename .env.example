# Used by `docker compose` (root docker-compose.yml). Copy to `.env` to override.
# Local (non-Docker) development uses backend/.env and frontend/.env.local instead.

# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=dev-only-secret-change-me-dev-only-secret-change-me
MONGO_DB_NAME=stocksense
ENVIRONMENT=development
COOKIE_SECURE=false

FIRST_ADMIN_EMAIL=admin@example.com
FIRST_ADMIN_PASSWORD=ChangeMe123!
FIRST_ADMIN_NAME=Administrator

NEXT_PUBLIC_APP_NAME=StockSense
