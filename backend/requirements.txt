# Runtime dependencies (direct dependencies, pinned). Upgrade with:
#   pip install -U -r requirements.txt   then update the pins from `pip freeze`
fastapi==0.141.1
uvicorn[standard]==0.53.0
pymongo==4.18.1          # ships the native AsyncMongoClient (>=4.13)
pydantic-settings==2.15.0
email-validator==2.3.0
PyJWT==2.14.0
bcrypt==5.0.0
